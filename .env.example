# Copy this to .env and fill in your details
# .env is gitignored — your data stays on your machine

# Optional: CapSolver API key for automated CAPTCHA solving
# Sign up at https://capsolver.com, deposit $5 (covers ~1,250 full opt-out runs)
# Leave blank to skip CAPTCHA solving (affected brokers fall back to manual_required)
CAPSOLVER_API_KEY=

# Optional: SMTP config for automated email opt-outs
# If not set, email opt-outs will be marked as manual_required
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
